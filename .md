# 多线任务管理工具 - Product Requirement Document

## Overview
- **Summary**: 本地优先的多项目并行任务管理 Web 应用，支持创建多个独立任务列表（项目），每个项目包含完整的任务管理功能，帮助用户同时跟踪多条工作线的进度。
- **Purpose**: 解决多线工作中任务混杂、难以并行管理的难题，提供清晰的项目划分和任务跟踪能力。
- **Target Users**: 需要同时处理多个项目/工作线的学生、自由职业者、办公室白领等多任务工作者。

## Goals
- 支持创建和管理多个独立项目（任务列表）
- 每个项目支持任务的完整生命周期管理
- 提供直观的任务状态跟踪（待办/进行中/已完成）
- 本地数据持久化，刷新不丢失
- 简单易用，无需复杂配置

## Non-Goals (Out of Scope)
- 用户注册/登录系统
- 云端数据同步
- 团队协作功能
- 拖拽排序功能
- 复杂的数据分析和报表
- 移动端原生应用

## Background & Context
- 用户希望通过简单的 Web 工具解决多线工作管理问题
- 优先考虑本地存储方案，避免后端开发和部署复杂度
- 基于现代前端技术栈实现，保证良好的用户体验

## Functional Requirements
- **FR-001**: 项目管理 - 支持创建、编辑、删除项目
- **FR-002**: 项目展示 - 多项目横向/纵向展示，每个项目显示任务完成数量（如"3/10"）
- **FR-003**: 任务管理 - 支持在项目内创建、编辑、删除任务项
- **FR-004**: 任务属性 - 任务项包含：标题（必填）、描述（可选）、优先级（高/中/低）、截止日期（可选）、标签（可选）
- **FR-005**: 任务状态流转 - 支持任务状态在待办/进行中/已完成之间循环切换
- **FR-006**: 本地持久化 - 使用 LocalStorage 存储所有数据，页面刷新后不丢失
- **FR-007**: 数据筛选 - 支持按任务状态和优先级筛选查看
- **FR-008**: 删除确认 - 删除项目时需要二次确认

## Non-Functional Requirements
- **NFR-001**: 性能 - 页面首次加载时间 < 1秒，操作响应时间 < 100ms
- **NFR-002**: 兼容性 - 支持 Chrome、Firefox、Safari 最新两版
- **NFR-003**: 可用性 - 无用户手册即可上手，界面简洁直观
- **NFR-004**: 可靠性 - LocalStorage 异常时优雅降级，提示用户清理数据
- **NFR-005**: 数据安全 - 数据仅存储在用户本地浏览器，不上传至任何服务器

## Constraints
- **Technical**: 
  - 前端框架：Vue 3（推荐）或 React 18
  - 构建工具：Vite
  - 数据存储：浏览器 LocalStorage
  - UI 组件库：Element Plus 或 Ant Design Vue
- **Business**: 
  - 开发周期：快速交付 MVP
  - 零运维成本：无需后端服务部署
- **Dependencies**: 
  - 浏览器支持 LocalStorage
  - 无外部 API 依赖

## Assumptions
- 用户使用现代浏览器（支持 LocalStorage 和 ES6+）
- 用户单个浏览器 LocalStorage 容量足够存储任务数据
- 无多设备同步需求
- 单人使用，无需权限控制

## Acceptance Criteria

### AC-1: 项目创建与展示
- **Given**: 用户打开应用
- **When**: 用户点击"新建项目"并输入项目名称
- **Then**: 新项目卡片成功创建并展示在界面上
- **Verification**: `programmatic`
- **Notes**: 验证项目列表 DOM 元素正确渲染

### AC-2: 项目删除与确认
- **Given**: 用户已创建至少一个项目
- **When**: 用户点击删除项目
- **Then**: 系统弹出二次确认对话框，确认后项目及其下所有任务被删除
- **Verification**: `programmatic`
- **Notes**: 验证 LocalStorage 数据同步删除

### AC-3: 任务创建与属性
- **Given**: 用户已创建一个项目
- **When**: 用户在项目内添加任务，填写标题、优先级、描述、截止日期、标签
- **Then**: 任务成功添加到该项目列表中，所有属性正确展示
- **Verification**: `programmatic`
- **Notes**: 验证任务数据结构包含所有字段

### AC-4: 任务状态流转
- **Given**: 用户有一个处于"待办"状态的任务
- **When**: 用户点击任务状态图标
- **Then**: 任务状态按"待办→进行中→已完成"循环切换，状态正确持久化
- **Verification**: `programmatic`
- **Notes**: 验证状态变更后 LocalStorage 同步更新

### AC-5: 完成数量统计
- **Given**: 项目内有 5 个任务，其中 2 个已完成
- **When**: 用户查看该项目卡片
- **Then**: 项目标题旁显示正确的完成统计（如"2/5"）
- **Verification**: `programmatic`
- **Notes**: 验证统计数字与实际任务状态一致

### AC-6: 本地数据持久化
- **Given**: 用户已创建项目和任务并刷新页面
- **When**: 页面重新加载
- **Then**: 所有项目和任务数据完整恢复，状态不变
- **Verification**: `programmatic`
- **Notes**: 验证 LocalStorage 读取和数据重建

### AC-7: 任务筛选功能
- **Given**: 项目内有不同状态和优先级的任务
- **When**: 用户选择筛选条件（如"仅显示高优先级进行中"）
- **Then**: 任务列表仅显示符合条件的任务
- **Verification**: `programmatic`
- **Notes**: 验证筛选逻辑正确性

### AC-8: 过期任务视觉提示
- **Given**: 任务截止日期已过
- **When**: 用户查看该任务
- **Then**: 任务卡片有明显的过期视觉标识（如红色边框或文字）
- **Verification**: `human-judgment`

### AC-9: 界面直观易用
- **Given**: 新用户首次打开应用
- **When**: 用户探索界面功能
- **Then**: 用户无需帮助即可理解并使用核心功能（创建项目、添加任务、切换状态）
- **Verification**: `human-judgment`

## Open Questions
- [ ] 是否需要项目颜色自定义？
- [ ] 是否需要任务归档功能？
- [ ] 是否需要导出/导入数据功能？
