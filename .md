# 多线任务管理工具
一个本地优先的多项目并行任务管理 Web 应用，帮助您同时跟踪多条工作线的进度。

## ✨ 功能特性

### 核心功能

- **多项目管理**：创建、编辑、删除多个独立项目（任务列表）
- **任务管理**：在每个项目内添加、编辑、删除任务
- **完整任务属性**：
  - 标题（必填）
  - 描述（可选）
  - 优先级（高/中/低）
  - 截止日期（可选）
  - 标签（可选）
- **任务状态流转**：待办 → 进行中 → 已完成（可循环切换）
- **完成数量统计**：每个项目显示任务完成情况（如 "3/10"）
- **本地数据持久化**：使用 LocalStorage 存储，刷新不丢失
- **任务筛选**：按状态和优先级筛选查看
- **过期任务提醒**：视觉标识过期任务

### 技术特点

- **现代前端技术**：Vue 3 + Vite
- **响应式设计**：适配不同屏幕尺寸
- **零依赖后端**：纯前端实现，无需服务器
- **高性能**：页面加载 < 1秒，操作响应 < 100ms
- **兼容性**：支持 Chrome、Firefox、Safari 最新两版

## 🚀 快速开始

### 环境要求

- Node.js 18.0 或更高版本
- npm 或 yarn

### 安装步骤

1. **克隆或下载项目**
2. **安装依赖**
   ```bash
   npm install
   ```
3. **启动开发服务器**
   ```bash
   npm run dev
   ```
4. **访问应用**
   打开浏览器访问 `http://localhost:5173`

### 生产构建

```bash
npm run build
```

构建产物将生成在 `dist` 目录。

## 📖 使用指南

### 1. 创建项目

- 点击页面顶部的 "新建项目" 按钮
- 输入项目名称
- 点击 "创建" 按钮

### 2. 管理项目

- **编辑项目**：点击项目名称旁边的编辑图标
- **删除项目**：点击项目卡片上的删除按钮（会有二次确认）

### 3. 添加任务

- 在项目卡片内点击 "添加任务" 按钮
- 填写任务信息（标题为必填）
- 点击 "保存" 按钮

### 4. 管理任务

- **切换状态**：点击任务左侧的状态图标（○ → ● → ✓）
- **编辑任务**：点击任务卡片上的编辑图标
- **删除任务**：点击任务卡片上的删除按钮

### 5. 筛选任务

- 使用顶部的筛选控件按状态和优先级筛选任务

## 📁 项目结构

```
todo_list/
├── src/
│   ├── App.vue              # 应用主组件
│   ├── main.js              # 应用入口
│   ├── styles.css           # 全局样式
│   ├── components/          # 组件目录
│   │   ├── ProjectCard.vue  # 项目卡片组件
│   │   ├── TaskItem.vue     # 任务项组件
│   │   └── ...
│   └── utils/               # 工具函数
│       ├── storage.js       # LocalStorage 存储服务
│       └── ...
├── index.html               # HTML 模板
├── package.json             # 项目配置
├── vite.config.js           # Vite 配置
└── README.md                # 项目说明
```

## 💾 数据存储

- 所有数据存储在浏览器的 LocalStorage 中
- 数据结构包括：
  - `projects`：项目列表
  - `tasks`：任务列表
  - `version`：数据版本（用于迁移）

## 🔧 技术栈

- **前端框架**：Vue 3
- **构建工具**：Vite
- **UI 组件**：Element Plus
- **状态管理**：Vue 3 Composition API
- **存储方案**：LocalStorage

## 📄 许可证

MIT License

## 🤝 贡献

欢迎提交 Issue 和 Pull Request 来改进这个项目！

## 📞 支持

如果您在使用过程中遇到问题，请：

1. 检查浏览器控制台是否有错误信息
2. 尝试清除浏览器缓存
3. 查看 LocalStorage 存储情况

***

**享受多线工作的高效管理！** 🎯
